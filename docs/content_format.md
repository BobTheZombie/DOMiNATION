# Content Format (JSON)

`game/content/default_content.json` schema:
- `resources: string[]`
- `ages: string[]`
- `cultures: {id:string, bonus:string}[]`

Future extension:
- `units[]` with stats and age unlocks.
- `buildings[]` with costs, production and prerequisites.
- `techs[]` with modifiers and dependencies.
